version: '3.4'

services: 
  frontend-react:
    build:
      context: '../frontend'
    ports: 
      - '3000:3000'
    volumes: 
      - '../frontend:/home/node/app:delegated'
    command: run start
    container_name: 'frontend'

  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: getitdone
    ports:
      - '5432:5432'


  backend:
    depends_on: 
      - db
    build:
      context: '../backend'
    ports: 
      - '8081:8081'
    environment:
      DATABASE_URL: postgres://user:pass@172.18.0.2:5432/db
    volumes: 
      - '../backend:/home/node/app:delegated'
    command:  sh -c "npm install && node ./bin/www"
    container_name: 'backend'